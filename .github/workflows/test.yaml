# MANAGED by EpiAwarePackageTools.scaffold — do not edit by hand.
name: Test

on:
  push:
    branches:
      - main
  pull_request:
  merge_group:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    uses: EpiAware/.github/.github/workflows/tests.yml@d024dc6927cc3523292a159dc0d4a45193c9c65d
    with:
      # The unregistered deps are pinned via `[sources]`, which needs
      # Julia >= 1.11, so the 1.10 lts cell cannot resolve. Drop it
      # until ConvolvedDistributions is registered.
      julia_versions: '["1", "pre"]'
    secrets: inherit  # pragma: allowlist secret
