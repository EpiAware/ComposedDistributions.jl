import{_ as o,o as t,c as a,an as d}from"./chunks/framework.Dk3HBPIQ.js";const u=JSON.parse('{"title":"Release notes","description":"","frontmatter":{},"headers":[],"relativePath":"release-notes.md","filePath":"release-notes.md","lastUpdated":null}'),n={name:"release-notes.md"};function c(r,e,i,s,l,p){return t(),a("div",null,[...e[0]||(e[0]=[d('<h1 id="Release-notes" tabindex="-1">Release notes <a class="header-anchor" href="#Release-notes" aria-label="Permalink to &quot;Release notes {#Release-notes}&quot;">​</a></h1><p>ComposedDistributions.jl follows a hybrid changelog approach:</p><ul><li><p><strong>GitHub Releases</strong>: All releases (automatically generated by TagBot)</p></li><li><p><strong>NEWS.md</strong>: Major releases requiring detailed context (shown below)</p></li></ul><p>See <a href="https://github.com/EpiAware/ComposedDistributions.jl/releases" target="_blank" rel="noreferrer">GitHub Releases</a> for minor/patch release information.</p><h2 id="Unreleased" tabindex="-1">Unreleased <a class="header-anchor" href="#Unreleased" aria-label="Permalink to &quot;Unreleased {#Unreleased}&quot;">​</a></h2><ul><li><p><strong>test:</strong> three new AD gradient scenarios close coverage gaps in the <code>ADFixtures</code> registry (<code>test/ADFixtures/src/ADFixtures.jl</code>): a <code>Shared</code>-tagged uncertain leaf driven through the full <code>logdensity</code> codec (tag-dedup&#39;s reverse-mode gradient accumulation was untested), a <code>Truncated</code>-wrapped uncertain leaf through the #216 leaf-wrapper registry&#39;s codec path, and a <code>Censored</code> leaf marginal (#215 landed with value-level tests only for both wrappers). Also ledgers a real, currently-CI-red regression: Enzyme reverse has crashed with an internal LLVM compiler error (<code>EnzymeInternalError</code> in <code>nodecayed_phis!</code>) on the &quot;Uncertain-leaf logdensity codec&quot; scenario since #190, reproducing on every CI run but not locally — declared broken in <code>backend_broken_scenarios()</code> so CI reflects the real state instead of failing outright (#223).</p></li><li><p><strong>docs:</strong> corrected <code>docs/benchmarks.md</code>&#39;s claim that the performance- history timeline updates &quot;on every push to <code>main</code> and on tagged releases&quot; (#231) — <code>benchmark-history.yaml</code> is currently parked to <code>workflow_dispatch</code>-only pending #41 (an unregistered, chained dependency the kit&#39;s benchmark scratch-registry bootstrap does not yet resolve), so the automatic trigger the page described is not actually running. <code>docs/benchmarks_notes.md</code> (the file that exists for exactly this kind of note) now explains the parked trigger and links #41, so a reader seeing &quot;not enough comparable revisions to compute ratios yet&quot; understands why rather than concluding the page is broken.</p></li><li><p><strong>feat:</strong> <code>register_leaf_wrapper!</code> is a new public hook so a leaf-wrapper package extension (ModifiedDistributions&#39; <code>Affine</code>/<code>Weighted</code>/<code>Transformed</code>/ <code>Modified</code>) can tell the generated flat-vector codec (<code>flat_dimension</code>, <code>unflatten</code>, <code>flatten</code>, <code>reconstruct</code>) how to peel its wrapper types and what extra parameters they own, without adding a direct dispatch method to <code>_leaf_free_type</code>/<code>_extra_names_of</code> — the codec&#39;s <code>@generated</code> functions cannot reliably see such a method if it is added after the generator has already compiled, a Julia <code>@generated</code>-function semantics gap confirmed by direct experiment (#188/#189). The hook takes plain data (a type-parameter index and a fixed extra-names tuple), never a callable, since even a stored closure hits the same world-age wall when called from a generator. A core (in-module) leaf wrapper (<code>Truncated</code>, <code>Distributions.Censored</code>) keeps its own direct-dispatch method but now routes its recursion through the same registry-aware resolver, so a core wrapper placed directly around a registered extension leaf (e.g. <code>truncated(thin(Gamma(...)))</code>) peels correctly too, not just the reverse nesting.</p></li><li><p><strong>chore:</strong> removed the <code>design/</code> folder (#224) — its one note, the time-/covariate-varying design rationale, is superseded by the landed <code>Varying</code>/<code>instantiate</code> implementation and its docstrings and the <a href="/previews/PR242/getting-started/varying-distributions#varying-distributions">Time-, strata-, and covariate-varying distributions</a> guide; dangling pointers to the removed file in <code>src/ComposedDistributions.jl</code>, <code>src/composers/varying.jl</code>, and that guide are cleaned up alongside it.</p></li><li><p><strong>chore:</strong> renamed <code>src/composers/hazard_one_of.jl</code> to <code>src/composers/Compete.jl</code> (#230), matching the file-per-type convention the other composers already follow (<code>Resolve.jl</code>, <code>Sequential.jl</code>, ...); no code changes, only the file name and its in-comment references.</p></li><li><p><strong>chore:</strong> renamed the public <code>_param_names</code>/<code>_leaf_ctor</code> leaf-protocol hooks to <code>param_names</code>/<code>leaf_ctor</code> (#229) — a leading underscore reads as &quot;internal&quot; in Julia, the opposite of what <code>public.jl</code> was declaring, and <code>docs/src/developer/leaf-protocol.md</code> already documented the clean names. The old underscored names remain as <code>const</code> aliases (the same function object, following the existing <code>uncertain_specs</code>/<code>_uncertain_specs</code> pattern), so an existing override such as <code>ComposedDistributions._param_names(::MyLeaf) = ...</code> keeps working.</p></li><li><p><strong>test:</strong> added a guard against <code>params_table</code>/codec ordering drift (#192, the #190 review follow-up): the runtime <code>params_table</code> walk and the generated <code>unflatten</code>/<code>flatten</code> codec are two independent, hand-maintained implementations of the same walk-order and dedup rule, and <code>ext/ComposedDistributionsDynamicPPLExt.jl</code> assumes their orderings coincide with nothing checking it. A new consistency test (<code>test/composers/codec_consistency.jl</code>) covers every composer/leaf shape; a new <code>as_turing</code>/NUTS round trip on a <code>shared(...)</code>-tagged tree (<code>test/composers/turing_ext.jl</code>) covers the one path that actually depends on the coupling. Full unification into one shared walker was assessed and set aside as disproportionate at this point (see the test file&#39;s header for the reasoning); <code>_param_names</code>/<code>_param_names_of</code>, the other duplicated piece the review flagged, turned out to serve genuinely different purposes (a public, instance-dispatched extension point vs. an internal, type-dispatched table the generated codec needs) and are not safely mergeable, so they get a direct comparison test instead.</p></li><li><p><strong>breaking:</strong> the <code>ComposedDistributionsLoweredDistributionsExt</code> extension and the <code>LoweredDistributions</code> weakdep are removed (LD#51, the #22 hub-owned decision). <code>LoweredDistributions</code> now hosts the <code>lower</code> bridge for the composer types itself, in its own <code>LoweredDistributionsComposedDistributionsExt</code> (moved verbatim from this package). Anyone who imported the extension module directly from this package must load <code>LoweredDistributions</code> and rely on its extension instead; functionality is otherwise unchanged when both packages are loaded together. This is the last source-bridge weakdep this package carries — the remaining <code>Bijectors</code>/<code>DynamicPPL</code>/<code>FlexiChains</code>/ <code>LogDensityProblems</code>/<code>Mooncake</code> inference extensions are separately staged to move out to DistributionsInference.jl (#185).</p></li><li><p><strong>chore:</strong> dropped the root <code>[sources]</code> pins for <code>ConvolvedDistributions</code> and <code>EpiAwareADTools</code> now that both are registered in General (ConvolvedDistributions 0.2.0, EpiAwareADTools 0.1.0) — both root dependencies resolve from the registry again, so this package is registrable (#41).</p></li><li><p><strong>feat:</strong> <code>reconstruct(d, x)</code> is a new generated primary for the flat-vector codec — a composed distribution rebuilt straight from an estimated flat vector in one call, with no intermediate <code>Dict</code>-typed accumulation (#178, PR 2 of the type-domain codec design). <code>unflatten</code>/<code>flatten</code>/<code>flat_dimension</code> are now themselves <code>@generated</code>: each walks the tree&#39;s TYPE once per distinct shape (names, tags and groups are compile-time since PR 1) and emits the slot indices as literals, producing a concretely-typed (<code>@inferred</code>-stable) nested <code>NamedTuple</code> instead of the old runtime <code>Dict{Symbol, Any}</code> walk. <code>logdensity</code> now routes through the generated <code>unflatten</code>, and this <strong>fixes #162</strong>: Enzyme reverse could not compile the old walk&#39;s type-unstable, heap-boxed reconstruction, and now differentiates it like every other backend. Measured on four baseline tree shapes (a plain chain, a tag shared across branches, a non-centred pooling group, and a Resolve stick-breaking node), the generated codec is 4.7-11.9x faster and allocates 4-14x less than the two-step <code>update(d, unflatten(d, x))</code> it replaces on the hot path; first-call compile latency for a fresh pooled+Resolve tree shape is unchanged (~0.6-0.7s, dominated by <code>Distributions</code>/<code>Dirichlet</code> compilation, not the generated function itself). The modifier-leaf codec path (<code>ModifiedDistributions</code>&#39; <code>Affine</code>/<code>Weighted</code>/ <code>Transformed</code>/<code>Modified</code>, e.g. <code>thin(...)</code>) is deferred to PR 4 (#189): a <code>@generated</code> function&#39;s generator can compile against a world snapshot taken before a later-loaded package extension adds its type-level leaf- protocol methods, a genuine Julia semantics gap around generated functions and extensions, not a bug in this PR&#39;s scope.</p></li><li><p><strong>breaking:</strong> the <code>ComposedDistributionsModifiedDistributionsExt</code> reverse extension and the <code>ModifiedDistributions</code> weakdep are removed (#170 step 2), ending the extension cycle between the two packages (Julia 1.12 fails on cross-module method overwrites when both packages&#39; extensions activate together). ModifiedDistributions&#39; own <code>ModifiedDistributionsComposedDistributionsExt</code> now hosts the full leaf protocol for its modifier leaves (<code>Affine</code> / <code>Weighted</code> / <code>Transformed</code> / <code>Modified</code>), reading it through the leaf-protocol public API published in #174. Anyone who imported the extension module directly from this package must load ModifiedDistributions and rely on its extension instead; functionality is otherwise unchanged when both packages are loaded together.</p></li><li><p><strong>refactor!:</strong> the composer/wrapper structs carry their layout-affecting names and tags in TYPE parameters rather than runtime fields, lifting <code>Sequential</code>/<code>Parallel</code>/<code>Choose</code>/<code>Resolve</code>/<code>Compete</code>&#39;s outcome/step/branch names, <code>Shared</code>&#39;s tag, and <code>Pool</code>&#39;s group/non-centred flag into the type domain (part of #178, PR 1 of the type-domain codec design). The public constructors (<code>Sequential(components, names)</code>, <code>shared(tag, dist)</code>, <code>pool(group, population; noncentred)</code>, ...) are UNCHANGED, and every consumer that already read through the accessors (<code>component_names</code>/<code>shared_tag</code>, plus the new <code>pool_group</code>/ <code>pool_noncentred</code>) needs no change. This is breaking only for code that constructed these structs directly and read the moved fields by name (e.g. <code>d.names</code>, <code>shared_leaf.tag</code>, <code>pool_spec.group</code>); such code should switch to the accessors. This groundwork is a step towards a generated, allocation-free flat-vector codec that fixes #162.</p></li><li><p><strong>fix:</strong> <code>as_logdensity</code> and the chain readback (<code>chain_to_params</code> / <code>update(template, chain)</code> / <code>param_draws</code>) now reject a tree where a <code>pool</code> group, a <code>shared</code> tag, and a root-level edge name are not disjoint (#177). All three land in the same root-lifted NamedTuple namespace at readback, so a same-named pair previously clobbered each other silently instead of erroring; the collision is caught once at construction or readback entry, not on the gradient hot path. Reusing one tag to tie a parameter across branches, or one group across several pooled members, is unaffected.</p></li><li><p><strong>feat:</strong> <code>update(d, x::AbstractVector)</code> is a flat-vector shorthand for <code>update(d, unflatten(d, x))</code>, collapsing an uncertain tree at a sampler draw in a single call (#178).</p></li><li><p><strong>feat:</strong> the composer leaf protocol is now published as considered public API (#170). The downstream contract a leaf-wrapper package implements is de-underscored and documented as the stable surface: <code>uncertain_specs</code>, <code>shared_tag</code>, <code>leaf_param_names</code>, <code>leaf_mean</code>, <code>leaf_var</code> and <code>leaf_detail_lines</code> join the already-public <code>free_leaf</code> / <code>rewrap_leaf</code> / <code>component_names</code> / <code>param_names</code> / <code>leaf_ctor</code> (the underscored spellings stay as internal aliases, so nothing breaks). The scalar <code>thin</code>-factor hook is generalised to a map-based <code>extra_leaf_params</code> / <code>set_extra_leaf_params</code> protocol, each extra parameter carrying its own value and support, which also fixes a latent <code>BoundsError</code> when a leaf carries an extra parameter alongside an uncertain native one. A new developer page documents the protocol. This release is non-breaking; the removal of the ModifiedDistributions reverse extension and its weakdep (ending the MD-CD extension cycle) follows in a later release.</p></li><li><p><strong>fix:</strong> <code>minimum</code> and <code>maximum</code> on a <code>Parallel</code> now return a per-branch NamedTuple of support bounds, matching how <code>mean</code> / <code>var</code> report per-endpoint moments, and the other composed types raise a clear <code>ArgumentError</code> naming the generic instead of the opaque <code>MethodError</code> about <code>iterate</code>. Also tightens the <code>Parallel</code> <code>event_names</code> test so it genuinely asserts the event tuple.</p></li><li><p><strong>feat:</strong> <code>@events</code> declares an event-tree TOPOLOGY as a readable operator diagram, structure only with no distributions attached (#156). <code>→</code> (<code>\\to</code>) chains events into a <code>Sequential</code>, <code>|</code> branches into a one_of outcome, <code>&amp;</code> runs branches in <code>Parallel</code>, and parentheses group for precedence; a bare identifier is an event name and becomes a named hole. <code>update(skeleton; name = dist, ...)</code> fills the holes and builds the concrete composed tree through the existing verbs, so one delay topology is reused across pathogens or settings. Whether a <code>|</code> node becomes a fixed-probability <code>Resolve</code> or a racing-hazard <code>Compete</code> is decided at fill time by the fill value type (<code>(dist, prob)</code> tuples versus bare distributions, the last branch alone free to take the residual), so <code>|</code> stays one syntax. A fill value is any valid leaf, including an <code>uncertain</code> / <code>@uncertain</code> leaf or a ModifiedDistributions modifier leaf, which composes through the existing extension with no MD-specific code in <code>@events</code>. The fill validates that every hole is filled and no unknown key is passed.</p></li><li><p><strong>docs:</strong> a &quot;Fitting a composed distribution&quot; guide walks the inference tooling in one place: the <code>as_logdensity</code> log-density over a tree&#39;s estimated parameters, sampling it through the <code>LogDensityProblems</code> interface with LogDensityProblemsAD, sampling with Turing through <code>as_turing</code>, and reading a fitted chain back onto the tree with <code>chain_to_params</code> / <code>update</code>.</p></li><li><p><strong>feat:</strong> <code>@uncertain expr</code> reads a distribution-valued constructor argument as that parameter&#39;s prior, so an uncertain composed tree reads naturally (#155). It rewrites syntax only, leaving the type sorting to the runtime positional <code>uncertain(D, args...)</code> method: each call <code>D(pos_args...)</code> whose head is a distribution type and one of whose positional arguments is a distribution literal becomes <code>uncertain(D, pos_args...)</code>, so <code>@uncertain Gamma(Normal(0.7, 0.2), 1.0)</code> makes <code>shape</code> uncertain and fixes <code>scale</code>. The walk is recursive, so it composes with <code>compose</code>, the verbs and the ModifiedDistributions wrappers (a modifier wraps the rewritten uncertain leaf); an all-literal constructor and a keyword-carrying or qualified call are left unchanged. Pure <code>Expr</code> rewriting, so no new dependency.</p></li><li><p><strong>test:</strong> extend the AD gradient scenarios (<code>test/ADFixtures</code>) across the ForwardDiff / ReverseDiff / Enzyme / Mooncake matrix. A new <code>:latent</code> category differentiates the full <code>as_logdensity</code>/<code>logdensity</code> codec over an uncertain-leaf tree (the flat-vector to nested-NamedTuple <code>unflatten</code>/ <code>update</code> path) and a centred pool (the <code>_pool_centred_logprior</code> population term), and the <code>:marginal</code> group gains a <code>Choose</code> scored at a selected alternative. The centred pool and the <code>Choose</code> differentiate on all four backends; the uncertain-leaf codec differentiates on ForwardDiff, ReverseDiff and Mooncake reverse but is marked broken on Enzyme reverse, which cannot compile its mixed fixed/active heap reconstruction (an opaque-pointer LLVM error).</p></li><li><p><strong>feat:</strong> <code>as_turing(dist, data; prefix, loglik)</code> builds a <code>DynamicPPL</code> model over a composed distribution&#39;s estimated parameters, so a composed posterior is sampleable with <code>sample(as_turing(dist, data), NUTS(), ...)</code> (#9). It is a light wrapper on the <code>as_logdensity</code> codec. Each estimated parameter is a named <code>~</code> site drawn from its own prior and the data likelihood is added with <code>@addlogprob!</code> from the codec&#39;s reconstruction, so the model&#39;s total log-density equals <code>logdensity(as_logdensity(dist, data), x)</code> by construction. The <code>~</code> site names match the inference readback exactly (<code>d.onset_admit.shape</code>, an uncertain node&#39;s <code>d.&lt;edge&gt;.branch_probs.stick_k</code>, a shared leaf once under its tag), so a fitted chain reads back through <code>chain_to_params</code> / <code>update(dist, chain)</code> unchanged. Supported rows carry a concrete prior (ordinary uncertain leaves and stick-breaking branch probabilities); a pooled tree is rejected for now (a centred pool has no fixed prior, and the readback does not yet consume a pooled chain), with a pointer to the <code>as_logdensity</code> + LogDensityProblemsAD path. The model lives in a new <code>ComposedDistributionsDynamicPPLExt</code> extension triggered by <code>DynamicPPL</code> alone.</p></li><li><p><strong>feat:</strong> a <code>LogDensityProblems</code> weak-dependency extension exposes a <code>ComposedLogDensity</code> (from <code>as_logdensity</code>) as a standard <code>LogDensityProblems</code> problem, so a composed distribution&#39;s posterior over its estimated parameters is sampleable by any LogDensityProblems consumer (AdvancedHMC, DynamicHMC, Pathfinder, Turing&#39;s <code>externalsampler</code>) with gradients supplied by LogDensityProblemsAD (#13). The extension implements <code>dimension</code> (the estimated flat-parameter count), <code>logdensity</code> (the codec&#39;s evaluator), and the zeroth-order <code>capabilities</code>. This is the Turing-free inference substrate that complements the DynamicPPL path, and it needs no new hard dependency.</p></li><li><p><strong>feat:</strong> a <code>LoweredDistributions</code> weak-dependency extension lowers a composed distribution to a backend-agnostic dynamical-systems representation, so <code>lower(compose(...))</code> yields a phase-type or continuous-time Markov chain for the whole delay structure that a Catalyst / ODE / Petri / Jump backend can consume (#149). The scalar composers lower exactly, since composition of phase-types is closed. <code>Sequential</code> convolves its steps into a series phase-type, <code>Resolve</code> mixes its outcomes into a hyper-phase-type weighted by the branch probabilities, <code>Compete</code> races its causes through the competing-risks Kronecker sum, and <code>Shared</code> lowers its wrapped leaf. The vector composers lower to a joint <code>CTMC</code>, <code>Parallel</code> through the Kronecker sum of its independent branches and <code>Choose</code> through the block-diagonal union of its selector alternatives. Nesting a <code>Parallel</code> or <code>Choose</code> inside a scalar composer raises a clear error rather than a silent misrepresentation.</p></li><li><p><strong>fix:</strong> <code>logdensity</code>/<code>unflatten</code> (<code>src/composers/logdensity.jl</code>) now differentiate under Mooncake, both forward and reverse. <code>unflatten</code> calls the <code>Symbol</code> path-splitter <code>_split_edge</code> unconditionally on every row, and the codec&#39;s length guards build their <code>DimensionMismatch</code> message by interpolating the tree object; both recurse into Base&#39;s UTF-8 string-indexing continuation machinery, for which Mooncake&#39;s whole-program rule derivation has no rule (a <code>sub_ptr</code> intrinsic hit), on every call for <code>_split_edge</code>, and on any reachable branch for the guards&#39; messages, regardless of whether it is taken. <code>_split_edge</code> and the four <code>DimensionMismatch</code>-throwing call sites (<code>logdensity.jl</code>, <code>named_outputs.jl</code>, <code>Parallel.jl</code>, <code>Sequential.jl</code>) are shielded from Mooncake with <code>@zero_derivative</code> in <code>ComposedDistributionsMooncakeExt</code> (#146).</p></li><li><p><strong>feat:</strong> <code>compose</code> gains a varargs-pairs spelling, <code>compose(:a =&gt; d1, :b =&gt; d2, ...)</code> (#145), a thin convenience over the primary <code>NamedTuple</code> form so call sites migrating from CensoredDistributions&#39; pairs-based <code>compose</code> keep working unmodified. See the FAQ for the migration note.</p></li><li><p><strong>feat:</strong> <code>_uncertain_specs</code> and <code>_leaf_detail_lines</code> are now <code>public</code> (#142), sanctioning the leaf-introspection recursion a leaf-wrapper package extends alongside <code>free_leaf</code>/<code>rewrap_leaf</code>. Without extending <code>_uncertain_specs</code>, an uncertain prior attached to a wrapped leaf was silently dropped by <code>build_priors</code>; without <code>_leaf_detail_lines</code>, a wrapped leaf&#39;s <code>inspect</code> detail fell back to its raw struct dump.</p></li><li><p><strong>test:</strong> pin three seams now that ConvolvedDistributions 0.2 is adopted: a Modified-wrapped chain step (affine / weight / thin) lowering through <code>observed_distribution</code> / <code>convolve_series</code> (#117); the recurrent-operator seam end to end (a time-varying chain resolved per step, collapsed, discretised, and driving the vector convolution surface, plus one hand-rolled renewal step, refs #82); and the restored real <code>cdf</code> assertion on a <code>difference</code> of two chains that the #122 workaround stood in for while ConvolvedDistributions #45 was open (#137).</p></li><li><p><strong>feat:</strong> <code>to_constrained(prob, z)</code> completes the PPL-neutral codec&#39;s HMC surface: given an assembled <code>ComposedLogDensity</code> and an unconstrained flat vector, it returns the constrained ESTIMATED parameters and the log-determinant Jacobian a sampler needs (<code>logdensity(prob, x) + logjac</code> is the unconstrained-space target). The transform is built per row from each row&#39;s prior via <code>Bijectors.bijector</code> (a stick-breaking <code>Beta</code> row, a positive-support prior, a non-centred pooled latent/hyperparameter), or, for a centred-pooled row, from its population&#39;s family. It lives in a new <code>ComposedDistributionsBijectorsExt</code> weakdep extension, so the core codec stays free of a <code>Bijectors</code> dependency.</p></li><li><p><strong>Breaking (upstream-driven):</strong> adopt the ConvolvedDistributions AD-seam move (#137): ConvolvedDistributions 0.2 relocated its AD-safe hook family out to the new <code>EpiAwareADTools</code> package under underscore-free names, so the racing-hazard node now calls and extends <code>EpiAwareADTools.logccdf_ad_safe</code> / <code>ccdf_ad_safe</code> (was <code>ConvolvedDistributions._logccdf_ad_safe</code> / <code>._ccdf_ad_safe</code>). <code>EpiAwareADTools</code> is a new dependency; it is unregistered, so it is git-pinned in the root and isolated (<code>test/ad</code>, <code>test/jet</code>, <code>benchmark</code>) environments until it registers. No user-facing API change.</p></li><li><p><strong>Breaking (upstream-driven):</strong> adopt ConvolvedDistributions 0.2, which makes the bare-distribution <code>convolve_series(delay, series)</code> discrete-only — a continuous delay now throws, because discretising it is an explicit modelling choice (single- vs double-interval censoring) upstream will not make silently (ConvolvedDistributions #31/#47). The composed-tree convenience is preserved: <code>convolve_series(::Sequential, series; events)</code> and <code>convolve_series(::Resolve/::Compete, series)</code> collapse to their continuous observed total and discretise it for you with the interval-censored-secondary scheme (<code>discretise_pmf</code> over lags <code>0:(length(series) - 1)</code>) before convolving, so the composed output is unchanged from before. For day-binned (double-interval-censored) primaries, discretise the total yourself and pass the PMF to <code>convolve_series(pmf, series)</code>. <code>discretise_pmf</code> and <code>DelayPMF</code> are now re-exported. Compat bumped to <code>0.2</code>; because 0.2 is unregistered the source is git-pinned (re-adding what #107 removed) until it registers.</p></li><li><p><strong>feat:</strong> re-export ConvolvedDistributions 0.2&#39;s Mellin product family (the <code>product</code> constructor for <code>Z = X * Y</code>), so the convolution surface is complete through ComposedDistributions alone. The <code>product</code> constructor is exported; the <code>Product</code> type stays unexported (a bare <code>Product</code> would clash with Distributions&#39; deprecated <code>Product</code>) but is public and reachable as <code>ComposedDistributions.Product</code>, mirroring ConvolvedDistributions. Composing a <code>Product</code> leaf into a tree is not yet wired (#139).</p></li><li><p><strong>fix:</strong> <code>probs</code> / <code>occurrence_probability</code> on a racing-hazard (<code>Compete</code>) node no longer return winning probabilities that sum slightly above one. The per-cause split is mathematically sub-stochastic (sums to <code>1 - ∏ S_k(∞) ≤ 1</code>), but Gauss-Legendre quadrature could overshoot to e.g. 1.0000322 for proper causes; the split is now rescaled to a valid probability vector when it exceeds one, leaving a genuine sub-one defective deficit intact (#115).</p></li><li><p><strong>refactor:</strong> the racing-hazard (<code>Compete</code>) moment, winning-probability and cause-cdf quadratures now call the public <code>integrate(::GaussLegendre, f, lo, hi)</code> rather than reaching into ConvolvedDistributions&#39; internal <code>GaussLegendre(; n).rule</code> and <code>gl_integrate</code>. Results are identical (the same fixed 64-node rule); this drops the coupling to an unexported upstream type that could change without a breaking bump (#109).</p></li><li><p><strong>refactor:</strong> renamed the internal <code>src/composers/intervene.jl</code> to <code>structural_edits.jl</code>, naming the <code>update</code> / <code>prune</code> / <code>splice</code> verbs it holds (the <code>intervene</code> verb is gone from the public API). No user-facing change (#114).</p></li><li><p><strong>test:</strong> end-to-end continuous delay-stack scenarios. A committed <code>test/composers/stack_scenarios.jl</code> testset drives a handful of named, epi-flavoured continuous stacks (an onset→admission→death <code>Sequential</code> chain, a <code>Parallel</code> of independent reporting branches, a death-vs-discharge <code>Resolve</code>, a competing-causes <code>Compete</code>, nested composes mixing them, a renewal <code>convolve_series</code>, a <code>tie</code> group and a <code>difference</code>) through the whole verb surface together — construction via both spellings, <code>rand</code>/<code>logpdf</code> round-trip, a seeded large-N Monte-Carlo moment check against the analytic / quadrature values, the introspection / edit / prior surface, and a ForwardDiff gradient per stack.</p></li><li><p>Overall moments of a composed tree now honour an <code>affine</code> modifier: a chain with an <code>affine(delay; scale, shift)</code> step reports the scale/shift-adjusted mean/var (matching what <code>rand</code> draws) instead of peeling the affine off to the inner delay&#39;s moment. A hazard-modified (<code>Modified</code>) leaf has no analytic moment yet, so a chain containing one now errors informatively rather than silently returning the unmodified free-leaf moment, pending ModifiedDistributions#44&#39;s numeric cumulative-hazard path (#120).</p></li><li><p><strong>Breaking (upstream-driven):</strong> following ConvolvedDistributions&#39; rename, the re-exported <code>convolve_distributions</code> is split into two verbs — <code>convolved(dists...; method)</code> for the distribution form (the sum <code>X + Y</code>, a chain&#39;s observed total) and <code>convolve_series(delay, series; interval)</code> for the timeseries form (convolving a numeric series through a delay). The composed-tree methods follow suit: <code>convolved(::Sequential)</code> collapses a chain to its total, and <code>convolve_series(::Sequential, series; events)</code> drives the renewal / latent series. No alias is kept (the package is unreleased).</p></li><li><p><code>sequential</code>, <code>parallel</code>, <code>resolve</code>, <code>compete</code> and <code>choose</code> now accept a positional <code>NamedTuple</code> spelling (<code>resolve((death = (Gamma(1.5, 1.0), 0.3), disch = Gamma(2.0, 1.5)))</code>) as the equivalent of the <code>name =&gt; value</code> Pairs, for hand-written children; <code>choose</code>&#39;s <code>selector</code> stays a keyword. The one_of constructors also build their outcome tuples with <code>map</code> rather than a generator comprehension, so constructing a <code>Resolve</code> / <code>Compete</code> inside a differentiated function is Enzyme-safe (no <code>collect_to!``Array</code> temporary Enzyme&#39;s type analysis rejects).</p></li><li><p><strong>Behaviour change:</strong> the standalone <code>(name, time)</code> view of a one_of draw is now a keyword on <code>rand</code>, <code>rand(node; outcome = true)</code>, rather than the separate public <code>rand_outcome</code>verb (now removed). A keyword-free<code>rand(node)</code>is unchanged (it still returns the full named event record), so only call sites that reached for the compact pair need updating from<code>ComposedDistributions.rand_outcome(rng, node)</code>to<code>rand(rng, node; outcome = true)</code>.</p></li><li><p><strong>Breaking:</strong> <code>rand</code> of a standalone <code>Resolve</code> or <code>Compete</code> node now returns the named event record of the outcome that fired — a <code>NamedTuple</code> keyed by <code>event_names(node)</code> (a positional origin slot then one slot per outcome, the fired outcome&#39;s time present and the others <code>missing</code>) — instead of the scalar marginal time-to-resolution (#96, syncing to CensoredDistributions&#39; #639). The record names which outcome occurred, so <code>logpdf(node, rand(node))</code> round-trips and identifies the outcome. To recover the old scalar draw, sample the marginal <code>rand(as_mixture(node))</code>; the <code>(outcome, time)</code> pair view is <code>rand(node; outcome = true)</code>. A one_of node nested inside a <code>Sequential</code> / <code>Parallel</code> is unchanged (it stays one scalar value slot, its marginal); a new <code>logpdf(node, ::NamedTuple)</code> scores a standalone record.</p></li><li><p><code>params_table</code> is now a superset schema carrying both the uncertain-first <code>prior</code> column and CensoredDistributions&#39; <code>:thin</code> rows via the <code>_thin_factor</code> / <code>_set_thin_factor</code> hooks (no-op here, so no <code>:thin</code> row appears; the hooks let a thinning modifier layer plug in). <code>_leaf_detail_lines</code> becomes the per-leaf <code>inspect</code> detail extension point, and the racing-hazard moment / winning-probability / cause-cdf quadratures thread a shared 64-node Gauss-Legendre rule (#96).</p></li><li><p>Partial pooling across strata (#78). A new <code>pool(group, population)</code> spec, placed inside <code>uncertain</code> where a prior would go, declares a parameter partially pooled across the leaves that name the same group: each member&#39;s parameter is drawn from one shared <code>population</code> distribution whose own free parameters are the estimated hyperparameters (carrying their priors through the ordinary <code>uncertain</code> spec machinery). It is the middle of the pooling spectrum between <code>shared</code>/<code>tie</code> (complete pooling, one value everywhere) and independent <code>uncertain</code> specs (no pooling, K unlinked values). A location-scale population (<code>Normal</code>/<code>LogNormal</code>) is reparameterised non-centred (one <code>Normal(0, 1)</code> latent per member, member <code>k</code> reconstructed as <code>mu + tau*z_k</code> or <code>exp(mu + tau*z_k)</code>), keeping the CensoredDistributions-compatible <code>[hyper..., z...]</code> flat layout; a general population takes the centred path (each member&#39;s parameter scored directly against the population). The hyperparameters flatten as ordinary uncertain-spec rows on the population.</p></li><li><p>Node-level uncertainty: a <code>Resolve</code>&#39;s branch probabilities can now be estimated (#89). Attach a simplex-valued <code>Distributions.Dirichlet</code> prior with <code>update(node, (branch_probs = Dirichlet(α),))</code>. The <code>Dirichlet</code> is what you write; the node is estimated through its K-1 stick-breaking coordinates (<code>:stick_1 … :stick_{K-1}</code>, each a <code>Beta</code> in (0, 1)), so <code>params_table</code>, the uncertain-first codec (<code>flatten</code> / <code>unflatten</code> / <code>flat_dimension</code> / <code>as_logdensity</code>) and chain readback all carry the sticks, and the probabilities are recovered from any draw (they always sum to one and the gradient is well-defined on every AD backend). Promote (<code>update(tree, param_priors(tree))</code>) attaches a flat <code>Dirichlet(ones(K))</code> per <code>Resolve</code>. <code>Compete</code>&#39;s winning probability is derived from the hazards and <code>Choose</code>&#39;s alternative is data-selected, so neither has a node-level free parameter (documented, no change).</p></li><li><p><code>convolve_series(chain, series; events)</code> convolves a timeseries to a named INTERIM event of a <code>Sequential</code> chain, not just its endpoint. The cumulative delay to an event is the observed collapse of the chain prefix up to it, so a single event name returns that event&#39;s count series and a tuple or vector of names returns a <code>NamedTuple</code> of series (the endpoint reproduces the whole-chain result). Only a plain continuous chain (every step a delay leaf) has per-event cumulative delays; a branching step is rejected, and an unknown event name errors listing the valid events. The discrete-event and thinning/branch-probability variants stay in CensoredDistributions.</p></li><li><p>See-through fitting of <code>Convolved</code> / <code>Difference</code> leaf component parameters, replacing the previous fixed-composite treatment. <code>params_table</code> now inventories each component&#39;s scalar parameters under a <code>component_i</code> path segment (e.g. <code>total.component_1.shape</code>), and <code>update</code> rebuilds the composite from the updated components (preserving the solver method). A component may be made <code>uncertain</code> in place, so the uncertain-first codec (<code>flatten</code> / <code>unflatten</code> / <code>flat_dimension</code> / <code>as_logdensity</code>) estimates a spec&#39;d component parameter like any other leaf parameter. The composite joins the shared <code>_node_children</code>/<code>_rebuild</code> deferred-leaf walk, so <code>has_uncertain</code>, <code>has_varying</code> and <code>instantiate</code> all see through a composite carrying an uncertain or varying component; it stays a single flat scored slot and an atomic node to the structural edits.</p></li><li><p>Reconciled the <code>Varying</code>/<code>instantiate</code> seam with the <code>Uncertain</code> machinery (#47): <code>Varying</code> and <code>Uncertain</code> are now presented as the two cases of one <em>deferred leaf</em> concept — a leaf that maps to a distribution and resolves later, <code>Varying</code> from an observed covariate (via <code>instantiate</code>) and <code>Uncertain</code> from a latent parameter draw (via <code>rand</code>/<code>update</code>). <code>instantiate</code> now rebuilds through the shared <code>_node_children</code>/<code>_rebuild</code> reconstruction machinery that <code>update</code> and the structural edits already use, and the <code>has_varying</code>/<code>has_uncertain</code> guards share one node walk, so resolution is no longer a separate hand-rolled tree traversal. No user-facing API change; <code>instantiate</code>, <code>update</code>, <code>has_varying</code>/<code>has_uncertain</code>, and the codec&#39;s rejection of an un-<code>instantiate</code>d <code>Varying</code> leaf are unchanged.</p></li></ul><h2 id="0.1.0-—-initial-release" tabindex="-1">0.1.0 — initial release <a class="header-anchor" href="#0.1.0-—-initial-release" aria-label="Permalink to &quot;0.1.0 — initial release {#0.1.0-—-initial-release}&quot;">​</a></h2><ul><li><p>The generic composition algebra ported from CensoredDistributions.jl: <code>compose</code> and the five composers (<code>Sequential</code>, <code>Parallel</code>, <code>Resolve</code>, <code>Compete</code>, <code>Choose</code>), <code>shared</code>/<code>tie</code>, structural edits (<code>update</code>/<code>prune</code>/<code>splice</code>), introspection (<code>params_table</code>, <code>build_priors</code>, <code>event</code>/<code>event_names</code>/<code>event_tree</code>), moments, and the convolution bridge (<code>observed_distribution</code>, <code>convolved</code>).</p></li><li><p>Added <code>Varying</code> / <code>Context</code> / <code>instantiate</code>: leaves whose distribution depends on an observed covariate (time, stratum), resolved by <code>instantiate(tree, ctx)</code>; <code>has_varying(tree)</code> guards fitting loops.</p></li><li><p><code>Resolve</code>/<code>Compete</code> outcome probabilities read via <code>Distributions.probs</code> (following the CensoredDistributions rename).</p></li><li><p>Docs site (overview, concepts, three tutorials, FAQ, interface contracts), benchmarks (core + AD) with a docs page, and a Mooncake AD extension.</p></li><li><p>Typed composer hierarchy: <code>AbstractComposedDistribution{F, S}</code> roots the composers, with <code>AbstractMultiChild{S}</code> grouping <code>Sequential</code>/<code>Parallel</code> and <code>AbstractOneOf</code> the univariate one_of family; downstream extension packages dispatch on these supertypes. The reusable <code>ComposedDistributions.TestUtils</code> harness (<code>test_interface</code>,<code>test_composed_interface</code>,<code>test_node_interface</code>,<code>test_abstract_membership</code>, ...) verifies a custom leaf or composer conforms to the interface.</p></li><li><p>Added <code>uncertain</code> / <code>Uncertain</code>: leaf distributions whose parameters are themselves distributions, nestable to any depth. <code>rand</code> draws the marginal so uncertain leaves compose everywhere, and the rest of the univariate surface (scalar <code>logpdf</code>/<code>cdf</code>/..., the moments, including a composed tree&#39;s overall moment) delegates to the template&#39;s central values. <code>has_uncertain(tree)</code> flags a tree that still holds an uncertain leaf, for a scoring/fitting loop to guard against a forgotten collapse. Collapse an uncertain leaf to its concrete template by pinning the parameters with <code>update(tree, params)</code>. Build one with a concrete template (<code>uncertain(Gamma(2.0, 1.0); shape = LogNormal(...))</code>), a positional family form (<code>uncertain(Gamma, LogNormal(...), 1.0)</code>), or the keyword family form. <code>truncated(uncertain(...))</code> pushes inside the template (conditional per-draw semantics). <code>params_table</code> gained a <code>prior</code> column carrying an uncertain parameter&#39;s spec, which <code>build_priors</code> now uses ahead of its per-row default.</p></li><li><p>Extended the ConvolvedDistributions verbs to composed trees: <code>convolve_series(chain, series)</code> convolves a timeseries (e.g. expected infections) through a <code>Sequential</code> chain&#39;s observed total delay (the renewal / latent observation layer), and <code>difference(a, b)</code> forms the difference of two chains&#39; observed totals; a <code>Parallel</code> / <code>Choose</code> (no single observed delay) errors with guidance. A <code>Convolved</code> / <code>Difference</code> node used as a leaf inside a tree scores, samples, and reports moments as a plain univariate leaf, and is treated as fixed structure by <code>params_table</code> / <code>build_priors</code> / <code>update</code> (fit its components by composing them as explicit chain steps).</p></li><li><p>Added the PPL-neutral LogDensityProblems core codec: the flat-vector &lt;-&gt; nested-NamedTuple bijection (<code>flatten</code> / <code>unflatten</code> / <code>flat_dimension</code>) and the assembled <code>ComposedLogDensity</code> (<code>as_logdensity</code> / <code>logdensity</code>), with no DynamicPPL / Turing dependency. A <code>ForwardDiff</code> gradient flows through <code>logdensity</code>.</p></li><li><p>Added the inference-readback verbs <code>chain_to_params</code> / <code>param_draws</code> / <code>strip_prefix</code> (and <code>update(template, chain)</code>): read a fitted chain&#39;s parameters back onto a composed-distribution template. Turing-free until both <code>DynamicPPL</code> and <code>FlexiChains</code> are loaded, when the extension supplies the methods.</p></li><li><p>Uncertain-first estimation: the <code>uncertain</code> specs set the estimation boundary. <code>flatten</code> / <code>unflatten</code> / <code>flat_dimension</code> / <code>as_logdensity</code> / <code>logdensity</code> target EXACTLY the spec&#39;d parameters, so a fixed leaf contributes no estimated dimension and a tree with no uncertain leaves estimates nothing (<code>flat_dimension == 0</code>; <code>logdensity</code> is then the data likelihood at the fixed tree). <code>update</code> introduces uncertainty through the prior interface: a distribution in a parameter slot makes just that parameter uncertain (a partial update), and <code>update(tree, param_priors(tree))</code> promotes a whole tree to uncertainty over its free parameters with default priors (the explicit estimate-everything path). <code>params_table</code> / <code>build_priors</code> are derived views over the object&#39;s specs; the readback verbs label exactly the spec&#39;d parameters.</p></li></ul><p>This file tracks notes for major releases and significant milestones; GitHub Releases (auto-generated from merged PRs) cover every release in between.</p>',9)])])}const m=o(n,[["render",c]]);export{u as __pageData,m as default};
